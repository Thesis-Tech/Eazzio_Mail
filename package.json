{
  "name": "eazzio-mail-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm --recursive run build",
    "lint": "npx prettier --check .",
    "test": "pnpm --recursive run test",
    "typecheck": "pnpm --recursive run typecheck",
    "mail:diagnose": "npx tsx scripts/diagnose-mail-provider.ts",
    "mail:test:local": "npx tsx scripts/test-local-mail.ts",
    "mail:test:external": "npx tsx scripts/test-external-mail.ts",
    "mail:test:authenticated": "npx tsx scripts/test-authenticated-mail.ts",
    "mail:test:authenticated-eazzio": "npx tsx scripts/test-authenticated-eazzio.ts",
    "mail:test:multi-user": "npx tsx scripts/test-multi-user-mail.ts",
    "mail:test:dual": "npx tsx scripts/test-dual-instance.ts"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "prettier": "^3.5.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
