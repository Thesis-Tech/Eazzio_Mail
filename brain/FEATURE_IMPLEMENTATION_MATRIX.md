# Eazzio Mail — FEATURE_IMPLEMENTATION_MATRIX.md

**Document Type:** Feature Implementation Reality Matrix  
**Parent Documents:** `Docs/PRD.md` v1.1 · `Docs/ARCHITECTURE.md` v1.2 · `Docs/TASKS.md` v2.0  
**Status:** Baseline Reality Audit (Task-001)

---

## 1. Feature Traceability Matrix

| FR/NFR ID | Requirement Summary | Expected Layer | Current Code Implementation | Status | Evidence in Code | Real Implementation Gap |
|---|---|---|---|---|---|---|
| **FR-AUTH-01** | Multi-tenant user registration (Argon2id) | `services/identity` | `IdentityService.register()` in `services/identity/src/application/identity-service.ts` | **PARTIAL** | Pure in-memory user instance returned; password hashed with Argon2id | Not wired to Postgres database or repository; user is not persisted to DB. |
| **FR-AUTH-02** | Password authentication & session JWT | `services/identity` | `IdentityService.authenticate()` in `services/identity/src/application/identity-service.ts` | **PARTIAL** | Password verified against hash; JWT signed via `TokenService` | Takes user object as in-memory parameter; no session row saved to `sessions` table. |
| **FR-AUTH-03** | Multi-Factor Authentication (TOTP) | `services/identity` | `TotpService` in `services/identity/src/domain/totp.ts` | **PARTIAL** | `otplib` used for verification; schema has `mfa_totp_secrets` | No TOTP setup flow, QR code generator, recovery codes, or DB secret encryption. |
| **FR-AUTH-04** | Session management & device revocation | `services/identity` | `SessionState` in `services/identity/src/domain/session-state.ts` | **NOT WIRED** | Pure memory active/revoked check logic | No active session querying API, no Redis/Valkey session cache, no DB update. |
| **FR-AUTH-05** | Account recovery & password reset | `services/identity` | `IdentityService.getRecoveryResponse()` | **PLACEHOLDER** | Returns static string message | No token generation, no reset email trigger, no expiry validation. |
| **FR-AUTH-06** | Role-Based Access Control (5-tier scopes) | `packages/domain` | `roles` table schema + `Policy` domain models | **PARTIAL** | SQL schema defines `roles` table with `scope_type`; domain models exist | No authorization middleware enforcing role hierarchy on routes. |
| **FR-AUTH-07** | Scoped API tokens | `packages/domain` | `api_tokens` table schema in `001_initial_schema.sql` | **NOT WIRED** | Table defined with scopes array & token hash | No token generation, validation, or header extraction middleware. |
| **FR-MBX-01** | Mailbox provisioning & quota allocation | `services/api` | `mailboxes` table schema in `001_initial_schema.sql` | **NOT WIRED** | Table defined; `Mailbox` domain entity exists | No API route or service logic to provision mailbox and associate domain. |
| **FR-MBX-02** | System & custom folders hierarchy | `services/api` | `MailboxService.getSystemFolders()` & `folders` table | **PARTIAL** | `GET /v1/mailboxes/:id/folders` returns static system folder array | Folders not fetched from or persisted to DB; custom folders unhandled. |
| **FR-MBX-03** | Non-duplicative labels & tagging | `services/api` | `MailboxService.applyLabels()` & `message_labels` table | **PARTIAL** | `POST /v1/mailboxes/:id/messages/:msgId/labels` returns 204 | No database persistence in `message_labels` table. |
| **FR-MBX-04** | Thread grouping & conversation view | `services/api` | `MailboxService.assignThread()` in `mailbox-service.ts` | **PARTIAL** | Normalized subject regex matching logic in TypeScript | Thread rows not inserted/updated in DB; message list returns empty `[]`. |
| **FR-MBX-05** | Quota calculation & soft/hard enforcement | `services/notification` | `NotificationService.checkQuota()` | **PARTIAL** | Calculates 90% threshold and creates payload | No DB trigger, no storage listener, no bounce trigger on hard quota. |
| **FR-DOM-01** | Custom domain management | `services/admin-service` | `domains` table schema | **NOT WIRED** | Schema defined in `001_initial_schema.sql` | No API routes to create, list, or delete custom domains. |
| **FR-DOM-02** | Strict 4-check DNS gate (MX, SPF, DKIM, DMARC) | `services/admin-service` | `DomainVerifier.evaluateStatus()` | **PARTIAL** | Pure function evaluates boolean object `{ mx, spf, dkim, dmarc }` | No real DNS resolver (`dns.promises`) to query actual nameservers. |
| **FR-DOM-03** | Domain aliases & disposable addresses | `packages/infra-adapters` | `domain_aliases` table schema | **NOT WIRED** | Schema with target mailbox FK defined | No alias routing, creation, or expiry enforcement logic. |
| **FR-IN-01** | Inbound SMTP reception & LMTP delivery | `services/mail-inbound` | `infra/docker/postfix`, `infra/docker/dovecot` | **NOT WIRED** | Base Alpine Dockerfiles created; `InboundEnvelope` defined | Postfix/Dovecot have no config files; no daemon listening to LMTP socket. |
| **FR-IN-02** | MIME parsing & attachment extraction | `services/mail-inbound` | `MimeParser.parse()` in `mime-parser.ts` | **PARTIAL** | Uses `mailparser` to parse MIME Buffer | Raw object not saved to MinIO/S3; attachment rows not saved to DB. |
| **FR-IN-03** | SPF, DKIM, DMARC, ARC evaluation gate | `packages/security-pipeline` | `decide()` in `packages/security-pipeline/src/decide.ts` | **COMPLETE** | Fully tested pure deterministic evaluation logic | Input auth results must be supplied by Rspamd/milter daemon (daemon unwired). |
| **FR-IN-04** | Rspamd spam scoring integration | `packages/security-pipeline` | `infra/docker/rspamd` Dockerfile | **NOT WIRED** | Base Dockerfile exists | No HTTP/milter client communicating between Node and Rspamd. |
| **FR-IN-05** | ClamAV antivirus attachment scanning | `packages/security-pipeline` | `infra/docker/clamav` Dockerfile | **NOT WIRED** | Base Dockerfile exists | No ClamAV clamd socket/TCP client scanning buffers. |
| **FR-IN-06** | Inbound filter execution & automated triage | `packages/domain` | `filters` table schema | **NOT WIRED** | JSONB conditions/actions schema defined | No filter engine evaluating rules against inbound messages. |
| **FR-OUT-01** | Mail composition & HTML sanitization | `services/mail-outbound` | `HtmlSanitizer` in `sanitizer.ts` | **PARTIAL** | Sanitizes script/onerror tokens; constructs basic MIME | No attachment handling, no template engine, no multipart/alternative structure. |
| **FR-OUT-02** | Cryptographic DKIM signing | `services/mail-outbound` | `DkimSigner` in `dkim-signer.ts` | **PARTIAL** | Injects `DKIM-Signature` header | Uses mock header injection rather than canonical RSA-SHA256 crypto signing. |
| **FR-OUT-03** | Outbound queue & exponential backoff | `services/mail-outbound` | `calculateNextAttempt()` in `backoff.ts` | **PARTIAL** | Backoff math implemented and tested | No background worker or BullMQ/Valkey queue runner polling `outbound_queue`. |
| **FR-OUT-04** | Bounce processing & delivery status | `services/mail-outbound` | `handleDeliveryAttempt()` in `outbound-service.ts` | **PARTIAL** | Generates event objects on max retries | No VERP bounce handler or DSN parser reading Postfix bounce logs. |
| **FR-SRCH-01** | Full-text message indexing (OpenSearch) | `services/search-indexer` | `SearchDocumentProjector` in `search-document.ts` | **PARTIAL** | Projects message event into search document schema | `IndexerService` uses uninstantiated interface; no live OpenSearch client. |
| **FR-SRCH-02** | Sub-second search API & autocomplete | `services/api` | `searchRouter` in `services/api/src/api/v1/search.ts` | **PLACEHOLDER** | `mockSearchAdapter` returns static dummy suggestions | No real OpenSearch query builder or connection pool. |
| **FR-REAL-01** | WebSocket realtime event gateway | `services/notification` | `NotificationChannelManager` in `domain/notification-channel.ts` | **PARTIAL** | Generates channel names and event payloads | No live `ws` or `Socket.io` gateway server running. |
| **FR-REAL-02** | SSE / Polling fallback | `services/notification` | Documented in ARCHITECTURE.md | **MISSING** | No SSE handler or fallback mechanism implemented | Need SSE endpoint or long-polling route. |
| **FR-AI-01** | AI thread summarization & reply suggestion | `services/ai-gateway` | `AiGatewayService` in `ai-service.ts` | **PARTIAL** | Policy check + adapter delegation logic implemented | `EazzioAI` adapter is an empty interface with no live Gemini/LLM integration. |
| **FR-AI-02** | AI organization opt-in policy gate | `services/ai-gateway` | `AiPolicyEvaluator` in `domain/ai-policy.ts` | **COMPLETE** | Tested unit logic verifying org policy flag | Opt-in policy enforced before AI invocation. |
| **FR-ADM-01** | Admin Portal (domains, mailboxes, orgs) | `apps/admin` | `apps/admin/README.md` | **MISSING** | Directory contains only a markdown README | Zero frontend application code or routes. |
| **FR-ADM-02** | Immutable append-only audit log | `packages/infra-adapters` | `audit_log` table & RLS in `002_rls_and_security_policies.sql` | **PARTIAL** | Table created, UPDATE/DELETE revoked, insert policy defined | No audit logging service intercepting API actions. |
| **NFR-SEC-01** | Multi-tenant isolation via Postgres RLS | `packages/infra-adapters` | `002_rls_and_security_policies.sql` | **PARTIAL** | Policies created for `mailboxes`, `messages`, `audit_log` | Missing policies for `folders`, `labels`, `threads`, `attachments`, `filters`, `domains`. |
| **NFR-SEC-02** | Zero-leakage client sanitization | `packages/security-pipeline` | `HtmlSanitizer` in `services/mail-outbound` | **PARTIAL** | Basic script/onerror stripping | Needs DOMPurify-grade sanitizer for inbound HTML message display. |
| **NFR-SEC-03** | Custom identity authority (no Supabase Auth) | `services/identity` | `services/identity` codebase | **COMPLETE** | Custom identity service with zero Supabase Auth dependencies | Identity logic fully owned by Eazzio. |
